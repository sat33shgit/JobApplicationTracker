// test-r2.js removed: R2 integration tests replaced by automated script in CI.
// This placeholder prevents accidental usage. If you need to re-run a local R2 test,
// recreate a purpose-built script under `scripts/` and ensure secrets are not checked in.

module.exports = {};
